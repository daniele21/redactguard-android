# Contributing

Use `dev` as the integration branch and focused workstream branches for parallel changes. Read `AGENTS.md` and `docs/current-state.md` before editing.

Changes should keep ownership explicit, preserve local-only privacy invariants, add tests at the cheapest deterministic level and avoid adding dependencies or abstractions without a concrete need.
